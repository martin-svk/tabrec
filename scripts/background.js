// Generated by CoffeeScript 1.8.0
(function() {
  'use strict';
  var API_URL, logger, notifier, rec;

  API_URL = 'http://tabber.fiit.stuba.sk';

  rec = new Recognizer;

  notifier = new Notifier;

  logger = new Logger(API_URL);

  rec.recognize();

  notifier.notify("Something happened!");

  logger.log("Logging this");

  logger.getUser(1);

}).call(this);
